export default defineEventHandler(async (event) => {
  // Web authentication middleware
  // This middleware handles session-based authentication for web routes
  // Leave empty for now - will be populated with session validation logic
})
