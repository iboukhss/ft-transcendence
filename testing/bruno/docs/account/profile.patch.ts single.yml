info:
  name: profile.patch.ts single
  type: http
  seq: 3

http:
  method: PATCH
  url: https://localhost:3000/api/account
  body:
    type: json
    data: |-
      {
        "houseNumber": 25
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
