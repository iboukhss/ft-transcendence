info:
  name: change-password.put.ts
  type: http
  seq: 3

http:
  method: PUT
  url: https://localhost:3000/api/auth/change-password
  body:
    type: json
    data: |-
      {
        "oldPassword": "newPassword",
        "newPassword": "myPassword",
        "confirmPassword": "myPassword"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
