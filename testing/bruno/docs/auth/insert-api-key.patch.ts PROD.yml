info:
  name: insert-api-key.patch.ts PROD
  type: http
  seq: 11

http:
  method: POST
  url: https://localhost:3001/api/auth/insert-api-key
  body:
    type: json
    data: |-
      {
        "userId": 26,
        "key": "some-new-secret-key",
        "name": "to be used with care",
        "isActive": true,
        "expiresAt": null
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
