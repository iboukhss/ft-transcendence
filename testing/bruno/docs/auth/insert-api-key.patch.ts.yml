info:
  name: insert-api-key.patch.ts
  type: http
  seq: 12

http:
  method: POST
  url: https://localhost:3000/api/auth/insert-api-key
  body:
    type: json
    data: |-
      {
        "userId": 2,
        "key": "some-new-secret-key",
        "name": "to be used with care",
        "isActive": true,
        "expiresAt": null
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
