info:
  name: login-admin.post.ts PROD
  type: http
  seq: 10

http:
  method: POST
  url: http://localhost:3001/api/auth/login
  body:
    type: json
    data: |-
      {
        "email": "d.heck@42.lu",
        "password": "d.heck@42.lu"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
