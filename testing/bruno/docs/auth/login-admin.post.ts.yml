info:
  name: login-admin.post.ts
  type: http
  seq: 7

http:
  method: POST
  url: https://localhost:3000/api/auth/login
  body:
    type: json
    data: |-
      {
        "email": "admin@42.lu",
        "password": "admin@42.lu"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
