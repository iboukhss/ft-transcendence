info:
  name: login-company.post.ts
  type: http
  seq: 2

http:
  method: POST
  url: https://localhost:3000/api/auth/login
  body:
    type: json
    data: |-
      {
        "email": "newEmail2@gmail.com",
        "password": "newPassword"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
