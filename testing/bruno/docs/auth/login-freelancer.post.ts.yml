info:
  name: login-freelancer.post.ts
  type: http
  seq: 5

http:
  method: POST
  url: https://localhost:3000/api/auth/login
  body:
    type: json
    data: |-
      {
        "email": "larue60@hotmail.com",
        "password": "password"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
