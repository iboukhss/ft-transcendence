info:
  name: update-api-key.patch.ts PROD
  type: http
  seq: 9

http:
  method: PATCH
  url: https://localhost:3001/api/auth/update-api-key
  body:
    type: json
    data: |-
      {
        "userId": 2,
        "key": "some-new-secret-key",
        "name": "to be used with care",
        "isActive": true,
        "expiresAt": null
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
