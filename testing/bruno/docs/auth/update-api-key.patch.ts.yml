info:
  name: update-api-key.patch.ts
  type: http
  seq: 6

http:
  method: PATCH
  url: https://localhost:3000/api/auth/update-api-key
  body:
    type: json
    data: |-
      {
        "userId": 14,
        "key": "some-new-secret-key",
        "name": "to be used with care",
        "is_active": true,
        "expires_at": null
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
