info:
  name: "[id].patch.ts"
  type: http
  seq: 3

http:
  method: PATCH
  url: https://localhost:3000/api/bookings/admin/1
  body:
    type: json
    data: |-
      {
        "price": 150
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
