info:
  name: profile.get.ts
  type: http
  seq: 2

http:
  method: GET
  url: https://localhost:3000/api/profile/profile
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
