info:
  name: profile.get.ts
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:3000/api/profile/profile
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
